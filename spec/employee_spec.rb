require 'spec_helper'


describe Employee do
  # it "adds the division id to the employee table" do
  #   new_project = Project.create({:name => "Project A"})
  #   new_division = Division.create({:name => "Division ABC"})
  #   new_employee = Employee.create({:name => "Bob"})
  #   add_division_id_to_employee(new_division.name, new_employee.name )
  #   expect(new_employee.division_id).to eq nil
  # end
end
